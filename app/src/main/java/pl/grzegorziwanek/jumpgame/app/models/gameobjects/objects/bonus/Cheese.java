package pl.grzegorziwanek.jumpgame.app.models.gameobjects.objects.bonus;

import android.graphics.Bitmap;

public class Cheese extends Bonus {
    public Cheese(Bitmap res, int x, int y, int width, int height, int speed, String type) {
        super(res, x, y, width, height, speed, type);
    }
}
